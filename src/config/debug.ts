// ---------------------------------------------------------------------------
// Debug flags — flip these locally to test features. Never commit as true.
// ---------------------------------------------------------------------------

/** Load cozy_test.json instead of the normal cozy room GLBs. */
export const DEBUG_USE_TEST_LEVEL = false;
